#include "main.h"
/**
 * _islower - Print a function for lowercase
 *
 * Description: int _islower(int c)
 *
 * Return: 0
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
		return (0);

}

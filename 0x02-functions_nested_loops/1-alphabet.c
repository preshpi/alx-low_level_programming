#include "main.h"
/**
 * main - print the alphabet in lowercase
 *
 *Return: 0
 */
int main(void)
{
	char lower;
	for (lower = 'a'; lower <= 'z'; lower++)
 		_putchar(lower);
	 	_putchar('\n');
return (0);	
}

#include "main.h"
/**
 * _abs - compute the absolutes value of an integer
 *
 * @n: number to be tested
 * Return: 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);

}
